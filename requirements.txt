# ──────────────────────────────────────────────────────────────────
# requirements.txt — Face Recognition Attendance System
# Install with:  pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────────

# Core vision
opencv-python>=4.8.0        # use opencv-python-headless on headless Pi
opencv-contrib-python>=4.8.0  # provides cv2.face.LBPHFaceRecognizer

# Face detection pipeline
mediapipe>=0.10.0

# Numerics
numpy>=1.24.0

# Raspberry Pi GPIO (comment out on non-Pi systems)
# RPi.GPIO>=0.7.0

# Optional: Tkinter GUI (usually pre-installed with Python on Pi)
# tk is part of the stdlib; no pip install needed
